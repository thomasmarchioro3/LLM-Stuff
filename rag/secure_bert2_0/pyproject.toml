[project]
name = "secure-bert2-0"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "torch>=2.11.0",
    "transformers>=5.5.4",
]

[tool.pyright]

venvPath = "."
venv = ".venv"

typeCheckingMode = "basic"
